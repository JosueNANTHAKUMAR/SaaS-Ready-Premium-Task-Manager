<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Epytodo - Portfolio Showcase</title>
    <link rel="stylesheet" href="/modern.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body>
    <div class="container center-screen">
        <div class="auth-container animate-fade-in">
            <!-- Login Card -->
            <div class="glass-panel auth-card">
                <h2 class="auth-title">Welcome Back</h2>
                
                <% if(locals.err_msg) { %>
                    <div class="alert alert-danger">
                        <%= err_msg %>
                    </div>
                <% } %>

                <form action="/login" method="post" style="width: 100%;">
                    <input type="email" class="glass-input" name="email" placeholder="Email" required>
                    <input type="password" class="glass-input" name="password" placeholder="Password" required>
                    <button type="submit" class="glass-btn" style="width: 100%; margin-top: 10px;">
                        Log In
                    </button>
                </form>
            </div>

            <!-- Register Card -->
            <div class="glass-panel auth-card">
                <h2 class="auth-title">New Here?</h2>
                <p style="margin-bottom: 30px; opacity: 0.8;">Join us and manage your tasks with style.</p>
                
                <form action="/register" method="post" style="width: 100%;">
                    <button type="submit" class="glass-btn" style="background: transparent; border: 2px solid var(--secondary-color); width: 100%;">
                        Create Account
                    </button>
                </form>
            </div>
        </div>
    </div>
</body>
</html>